#include<iostream>
#include<stdio.h>
#include<string.h>
#include<math.h>

using namespace std;

int main()
{
    int no_of_test, i, T, Q, N, str[100002] ,a, b;

    cin>>no_of_test;
    while(no_of_test--)
    {
        cin>>N;
        for(i=0;i<N;i++)
        {
            cin>>str[i];
        }
        cin>>Q;

    }
    return 0;
}
